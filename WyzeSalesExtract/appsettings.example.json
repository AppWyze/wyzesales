{
  "Source": {
    "_comment": "Which client's extractor to run (see WyzeSalesExtract/Extraction). Optional - defaults to \"WCSA\" when left out entirely, so this section can just be deleted for a WCSA-pattern client's config rather than filled in. Set to \"EDGETEC\" to run EdgetecSourceExtractor instead - see the Edgetec section below, and note Database (above) is then unused/ignored.",
    "Type": "WCSA"
  },
  "Database": {
    "_comment": "WCSA/IQRetail only - ignored when Source.Type is \"EDGETEC\" (see the Edgetec section below instead).",
    "Dsn": "IQNew",
    "ConnectionString": null,
    "BasePath": "C:\\IQRetail\\IQEnterprise\\002"
  },
  "Edgetec": {
    "_comment": "Only used when Source.Type is \"EDGETEC\" (ignored otherwise - safe to leave as-is for WCSA-pattern clients). Dsn is the ODBC DSN name for Edgetec's own driver (the original QlikView script's own extract used DSN \"Edgetec64\") - set ConnectionString instead if a DSN-less connection string is preferred, in which case Dsn is ignored. BasePath is the UNQUOTED path prefix Edgetec's ODBC driver expects in its own FROM clauses (e.g. \"Z:\" - Edgetec's driver differs from WCSA/IQRetail's, which needs a quoted path - see Data/EdgetecDb.cs). FilesPath is the folder holding the three flat files this extractor also reads directly off disk: STOCK.TXT, ACCOUNTS.TXT, and \"Edgetec Formats.xlsx\".",
    "Dsn": "Edgetec64",
    "ConnectionString": null,
    "BasePath": "Z:",
    "FilesPath": "Z:\\QlikView"
  },
  "Supabase": {
    "_comment": "Use the Session-mode pooler connection string from Supabase's project settings (Database > Connection string). ClientCode identifies which row in the clients table this data belongs to - created automatically on first run if it doesn't already exist.",
    "ConnectionString": "",
    "ClientCode": "WCSA"
  },
  "Schedule": {
    "_comment": "24-hour times, server local time. Add/remove entries as needed - the running service picks up changes to this list without needing a restart, except a change only takes effect once the currently-awaited run fires (restart the service for an immediate change). Runs whether or not anyone is logged in, once installed as a service - see README.",
    "RunTimes": [ "06:00", "14:00" ],
    "RunOnStartup": false
  },
  "FiscalYear": {
    "_comment": "Leave OverrideYear null to auto-compute (Jan/Feb belong to the fiscal year that started the previous March; Mar-Dec belong to the fiscal year ending next Feb).",
    "OverrideYear": null
  },
  "DataWindow": {
    "_comment": "Optional - leave EarliestLoadDate null (the default) for a client with no separate prior extract to protect, like WCSA. Set it only when onboarding a client from an already-verified prior extract (see README 'Adding a new client' step 5) - e.g. Edgetec's is \"2026-09-07\", the first date NOT already covered by its prior verified data. Once set, no run can ever delete or overwrite anything before this date.",
    "EarliestLoadDate": null
  },
  "Logging": {
    "LogFolder": "C:\\WyzeSalesExtract\\Logs"
  }
}
